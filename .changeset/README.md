# Changesets

This folder contains changeset files that track changes for versioning and changelog generation.

When you make a change, run `pnpm changeset` to create a changeset file describing your changes.

